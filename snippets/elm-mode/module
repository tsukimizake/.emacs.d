# -*- mode: snippet -*-
# name: module
# key: module
# --
module `
(progn
  (require 'f)
  (require 's)
  (let ((modulename (f-no-ext (f-filename (buffer-file-name)))))
    (upcase-initials modulename)))
` exposing (..)